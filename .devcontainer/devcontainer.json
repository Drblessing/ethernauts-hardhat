{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {}
  },
  // Use 'postCreateCommand' to run commands after the container is created.
  "postCreateCommand": "npm install --save-dev hardhat && npm install --save-dev @nomicfoundation/hardhat-toolbox@^2.0.0 && npm install dotenv && npm install --save-dev @nomiclabs/hardhat-etherscan"
}
